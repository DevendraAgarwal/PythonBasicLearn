'''
How to create a module?

Create a new file:
    Create a new file with a .py extension (e.g., my_module.py).

Define functions and variables:
    Write your code in the file, defining functions, 
    variables, and classes as needed.

Save the file:
    Save the file in a directory that is included in your 
    Python’s sys.path (e.g., site-packages or lib).

How to import a module?

Use the import statement:
    In your Python script or interactive shell, use the import statement to load 
    the module. For example: import my_module.

Access module contents:
    Once imported, you can access the module’s contents 
    (functions, variables, and classes) using dot notation.
    For example: my_module.my_function() or my_module.my_variable.

How to import variants?

import module_name:
    Imports the entire module, making its contents available in the current 
    namespace.

from module_name import item:
    Imports a specific item (function, variable, or class) from the module,
    making it available in the current namespace.

import module_name as alias:
    Imports the module and assigns it an alias 
    (e.g., import my_module as mm).
'''